# 12: Graduate: reused Interview Practice

**What to build:** Graduate dashboard's Interview Practice imports `InterviewSetup`/`AIInterviewSession`/`InterviewReport` directly from `student/prepareforfuture` — no duplicated components.

**Blocked by:** 11

**Status:** done

- [x] Graduate can run a full mock interview session using the imported components
- [x] No copy-pasted interview-prep component exists under `graduate/`
- [x] A fix/change to the shared component reflects on both student and graduate sides automatically (verified by inspection, not duplication)
